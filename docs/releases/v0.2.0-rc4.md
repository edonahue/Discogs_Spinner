# discogs_player v0.2.0-rc4

## Summary

- Release type: `rc`
- Tag: `v0.2.0-rc4`
- Date: `2026-02-26`
- Scope: cross-platform tagged-release pipeline hardening (macOS build-path fixes) and release-gate progression for pilot rollout.

## Highlights

- Fixed tagged-release pipeline failures on macOS in `scripts/build_artifacts.sh`:
  - portable temp directory creation (`mktemp` GNU/BSD compatibility),
  - Bash `set -u` safe handling of optional wheel arguments.
- Re-ran tagged-release automation on `v0.2.0-rc4` and reached full green:
  - Ubuntu artifacts: success,
  - Windows artifacts: success,
  - macOS artifacts: success,
  - publish job: success.
- Opened post-release validation tracking issue:
  - `https://github.com/edonahue/Discogs_Spinner/issues/1`

## Packaging And Distribution

- GitHub release:
  - `https://github.com/edonahue/Discogs_Spinner/releases/tag/v0.2.0-rc4`
- Workflow run:
  - `https://github.com/edonahue/Discogs_Spinner/actions/runs/22426315828`
- Assets:
  - `CHECKSUMS.ALL.txt`
  - `discogs_player-core-linux-x86_64.tar.gz`
  - `discogs_player-core-macos-arm64.tar.gz`
  - `discogs_player-core-windows-x86_64.tar.gz`
  - `discogs_player-plus-linux-x86_64.tar.gz`
  - `discogs_player-plus-macos-arm64.tar.gz`
  - `discogs_player-plus-windows-x86_64.tar.gz`

## Install And Setup

- Windows quickstart: `docs/quickstart_windows.md`
- Debian quickstart: `docs/quickstart_debian.md`
- macOS quickstart: `docs/quickstart_macos.md`

## Validation Evidence

- `bash ./scripts/prepublish_hygiene_check.sh`: pass
- `venv/bin/ruff check .`: pass
- `venv/bin/python -m mypy src/discogs_player --show-error-codes --hide-error-context`: pass (`92` files)
- `venv/bin/python -m pytest -q`: pass (`369 passed, 3 skipped`)
- `npm --prefix webapp run build`: pass
- `./scripts/gui_smoke_test.sh 12`: pass
- `./scripts/gallery_ux_smoke.sh 12`: pass

## Known Limitations

- Native signed installers are not yet provided (current release assets are tarball bundles).
- Manual pilot-user FTUX validation on clean Windows/macOS machines is still required.
- macOS Gatekeeper/notarization remains a documented future-hardening area.

## Reporting Issues

When filing issues, include:

- OS and install path
- reproduction steps
- `dplayer diagnostics --json`

Issue templates:

- install failure
- auth/setup failure
- playback failure

---

Runbook reference:

- `docs/RC_RELEASE_RUNBOOK.md`
