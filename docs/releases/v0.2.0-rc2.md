# discogs_player v0.2.0-rc2

## Summary

- Release type: `rc`
- Tag: `v0.2.0-rc2`
- Date: `2026-02-26`
- Scope: documentation and release-readiness follow-up after `v0.2.0-rc1`, including checkpoint consistency fixes and first checklist/runbook execution evidence.

## Highlights

- Updated public docs and product-state references for consistency:
  - synced latest validation/report links,
  - clarified checkpoint wording as captured-state vs live-state,
  - appended post-checkpoint push/tag updates.
- Added first checklist/runbook execution evidence:
  - `docs/RELEASE_CHECKLIST_FIRST_PASS_2026-02-26.md`
  - recorded passing local gate commands (`hygiene`, `ruff`, `mypy`, `pytest`, web build, GUI smoke, gallery smoke).
- Confirmed repository synchronization:
  - `master` pushed to `origin/master`,
  - existing `v0.2.0-rc1` tag pushed to GitHub,
  - new `v0.2.0-rc2` tag created from current `master` head.

## Packaging And Distribution

- Intended artifacts:
  - `discogs_player-core-<os>-<arch>.tar.gz`
  - `discogs_player-plus-<os>-<arch>.tar.gz`
- Intended checksum manifest:
  - `CHECKSUMS.ALL.txt`
- Build workflow:
  - `Tagged Release` (`.github/workflows/tagged_release.yml`)

## Release Automation Status (Tag Push Run)

- Workflow run: `Tagged Release` run #2
- Trigger: push of tag `v0.2.0-rc2`
- Run URL: `https://github.com/edonahue/Discogs_Spinner/actions/runs/22425924450`
- Result: `failure`
- Failed job: `Build Release Artifacts (macos-latest)`
- Failed step: `Build core and plus artifacts`
- Publish job (`Publish GitHub Release`): `skipped`
- GitHub Release object for `v0.2.0-rc2`: not created (`404` when queried by tag)

## Install And Setup

- Windows quickstart: `docs/quickstart_windows.md`
- Debian quickstart: `docs/quickstart_debian.md`
- macOS quickstart: `docs/quickstart_macos.md`

## Validation Evidence

- `prepublish_hygiene_check.sh`: pass
- `ruff`: pass
- `mypy`: pass (`src/discogs_player`, 92 files)
- `pytest -q`: `369 passed, 3 skipped`
- web build: pass (`npm --prefix webapp run build`)
- GUI smoke: pass (`./scripts/gui_smoke_test.sh 12`)
- Gallery UX smoke: pass (`./scripts/gallery_ux_smoke.sh 12`)

## Known Limitations

- Tagged-release automation currently fails on `macos-latest` during artifact build, blocking automatic release publication.
- Because publish job is skipped after that failure, no GitHub Release/assets are attached for this tag yet.
- Desktop deliverables remain tarball artifacts (no signed native installers/notarized macOS package in this RC).

## Upgrade / Migration Notes

- No DB schema migration required for this RC.
- No breaking CLI/API contract changes introduced between `v0.2.0-rc1` and `v0.2.0-rc2`.

## Reporting Issues

When filing issues, attach:

- `dplayer diagnostics --json`
- reproduction steps
- OS + install path details

Issue templates:

- install failure
- auth/setup failure
- playback failure

---

Runbook reference:

- `docs/RC_RELEASE_RUNBOOK.md`
