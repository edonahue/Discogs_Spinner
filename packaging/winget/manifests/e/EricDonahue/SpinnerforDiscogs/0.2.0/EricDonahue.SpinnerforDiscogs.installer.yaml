# yaml-language-server: $schema=https://aka.ms/winget-manifest.installer.1.6.0.schema.json
#
# IMPORTANT: Before submitting a PR to microsoft/winget-pkgs, replace the
# placeholder SHA256 values below with the actual checksums of the release
# artifacts. Compute them with:
#
#   certutil -hashfile <file> SHA256   (Windows)
#   sha256sum <file>                   (Linux/macOS)
#
# Release artifacts are at:
#   https://github.com/edonahue/Discogs_Spinner/releases/tag/v0.2.0
#
PackageIdentifier: EricDonahue.SpinnerforDiscogs
PackageVersion: 0.2.0
MinimumOSVersion: "10.0.17763.0"
InstallerType: nullsoft
InstallModes:
  - interactive
  - silent
UpgradeBehavior: install
Installers:
  - Architecture: x64
    InstallerUrl: https://github.com/edonahue/Discogs_Spinner/releases/download/v0.2.0/Discogs.Spinner_0.2.0_x64-setup.exe
    InstallerSha256: PLACEHOLDER_SHA256_REPLACE_BEFORE_PR_SUBMISSION
    InstallerType: nullsoft
  - Architecture: x64
    InstallerUrl: https://github.com/edonahue/Discogs_Spinner/releases/download/v0.2.0/Discogs.Spinner_0.2.0_x64_en-US.msi
    InstallerSha256: PLACEHOLDER_SHA256_REPLACE_BEFORE_PR_SUBMISSION
    InstallerType: wix
ManifestType: installer
ManifestVersion: 1.6.0
